package controllers.animations;

/**
 * Shared constants to coordinate state between the controller and animation helpers.
 */
public final class AnimationConstants {
    public static final String OVERLAY_TICK_LABELS_KEY = "overlayTickLabels";

    private AnimationConstants() {
        // no instances
    }
}
